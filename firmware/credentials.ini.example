[credentials]
; Wi-Fi credentials are no longer set here — the firmware runs a captive
; portal AP ("XTE-DCDU-Setup") on first boot and stores the credentials
; you enter in NVS.
; The only build-time secret left is the OTA upload password (optional).
ota_password =
